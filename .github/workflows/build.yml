name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'temurin'
      - name: Install Gradle
        run: sudo apt update && sudo apt install gradle -y
      - name: Build
        run: gradle build
      - uses: actions/upload-artifact@v4
        with:
          name: client
          path: build/libs/*.jarAdd workflows
